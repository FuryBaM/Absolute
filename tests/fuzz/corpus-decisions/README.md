# Decision seeds

The analyzer harness spends its input as choices in a program generator,
so these are decision streams rather than Absolute source. Set
`ABSOLUTE_FUZZ_DUMP_SOURCE=1` to print the program a buffer stands for.
