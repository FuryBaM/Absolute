int32 main() {
    string msg = "hello";
    string forwardedMsg = forward(msg);
    assert(forwardedMsg == "hello", "forward lvalue string");

    string temp = forward("world");
    assert(temp == "world", "forward rvalue string");

    println("forward-builtin=ok");
    return 0;
}
