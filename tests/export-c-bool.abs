export "C" bool absolute_is_positive(int32 value) {
    return value > 0;
}

int32 main() {
    assert(absolute_is_positive(3), "positive");
    assert(!absolute_is_positive(-1), "negative");
    println("export-bool=ok");
    return 0;
}
