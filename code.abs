int main() {
    int size = 8;
    int board[size][size];
    for (int y = 0; y < size; y += 1) {
        for (int x = 0; x < size; x += 1) {
            board = x + y;
        }
    }
    return 0;
}